{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "nixpacksConfig": {
      "aptPkgs": ["postgresql-client"]
    }
  },
  "deploy": {
    "startCommand": "php artisan optimize:clear ; php -S 0.0.0.0:${PORT:-8080} -t public"
  }
}
